anthropic>=0.40.0
rich>=13.0.0
python-dotenv>=1.0.0
psutil>=5.9.0
pyperclip>=1.8.0
requests>=2.31.0

# Bandeja del sistema y ventana de chat GUI (opcional, activar con --tray):
# pystray>=0.19.0
# Pillow>=10.0.0

# ADK backend con Claude (JARVIS_BACKEND=adk):
# google-adk>=1.0.0
# litellm>=1.0.0

# ADK backend con Gemini (JARVIS_BACKEND=gemini):
# google-adk>=1.0.0
# (no requiere litellm)

# Voz (JARVIS_VOICE_ENABLED=true):
# SpeechRecognition>=3.10.0
# pyttsx3>=2.90
# pyaudio>=0.2.13
